const { generateWave } = require('./generateWave');
const {generateBarData}=require('./generateBarData');
const {generatTideData}=require('./generateTideData');
const {generateAreaData}=require('./generateAreaData');

module.exports = {
    generateWave,
    generateBarData,
    generatTideData,
    generateAreaData
}